# java-backend

## Day 1 Learning

Today I learned about the fundamentals of Java and programming. I learned what a high-level language and a low-level language are, and what the difference between them is. I learned about what bytecode and JVM are. I have seen what the difference is between a Compiler and an Interpreter. I understand why Java uses both a Compiler and an Interpreter for generating code. The Compiler is used to compile the Java source code into bytecode, and the just-in-time Interpreter is used to convert the bytecode into machine-level code on different machines. I also learned about the basic syntax of Java and how to write a simple program.

---

## Day 2 Learning

Today I have learned about:
 1. fetch-execute cycle in which the CPU fetches and executes the program in small chunks. Fetch --> Ram --> Decode --> Execute 
 2. Asynchronous events, events or signals that occurs unexpectedly during the execution of the program.
    * There are two types:
        1. Interrupts: signal generated by hardware to get attention of CPU 
        2. Polling : Continuously checking for the events and checking for the changes.
 3. Sub-routings: Another name of the method is sub-routings. 

---

## Day 3 Learnings

Today I have learned about:
 1. **Different types of variables** and the difference between `print` and `println`.
 2. Wrote a code for calculating simple intrest and 
 3. Created a class named `student` with `getDetails` method to print the details of the student.
 4. Diff. b/w class n objects
 5. Tokens, syntax n semantics

---

## Day 4 Learnings

Revised Previous Tasks  

## Day 5 Learnings

Today I have learned about:
- What is a static method and an instance method?
- What are Scanner objects?
- What is a string?
- String pooling
- Why do we import packages in Java?
- Use of .equals() method
- Diff. b/w == and .equals()
- Primitive and non-primitive data types
- Created analogies on methods

Completed all the theory part of task 3. 



































































.